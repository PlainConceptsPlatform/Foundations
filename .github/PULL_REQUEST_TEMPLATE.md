## What changed

<!-- One or two sentences. What a reviewer needs to know before reading the diff. -->

## Why

<!-- The problem this solves. Link the issue or the conversation if there is one. -->

## Checklist

- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` and `pnpm build` pass locally
- [ ] Generated content is current (`pnpm --filter @plainconceptsplatform/docs gen`) if a generator's input changed
- [ ] Screenshots below for anything visible, in **both** light and dark
- [ ] A changeset (`pnpm changeset`) if `packages/**` changed, since that ships to npm
- [ ] Docs updated in the same PR if this changes a convention other teams follow

## Screenshots

<!-- Light and dark. Delete this section if the change is not visible. -->

## Notes for the reviewer

<!-- Anything deliberately left out, a follow-up you plan, or a decision you want challenged. -->
