---
title: "Popover"
description: "Displays rich content in a portal, triggered by a button."
---

import { PopoverDemo } from "@/components/previews/popover-demo"

## Installation

```bash
npx shadcn@latest add popover
```

## Usage

```tsx
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"

export function PopoverDemo() {
  return (
    <Popover>
      <PopoverTrigger asChild>
        <Button variant="outline">Open popover</Button>
      </PopoverTrigger>
      <PopoverContent className="w-72">
        <div className="grid gap-3">
          <Label htmlFor="width">Width</Label>
          <Input id="width" defaultValue="100%" />
        </div>
      </PopoverContent>
    </Popover>
  )
}
```

## Preview

<div className="my-4 flex flex-wrap items-center gap-4 rounded-lg border border-border p-6">
  <PopoverDemo />
</div>

## Full API

[ui.shadcn.com/docs/components/popover](https://ui.shadcn.com/docs/components/popover)
