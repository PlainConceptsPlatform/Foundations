---
title: "Date Picker"
description: "A themed date range picker built on the Calendar and Popover primitives."
---

import { DatePickerDemo } from "@/components/previews/date-picker-demo"

## Installation

Date Picker is a composite pattern, not a shadcn registry item. It assembles two
primitives whose dependencies are already present (`react-day-picker` and
`radix-ui`):

```bash
npx shadcn@latest add calendar popover
```

The preview wraps `Calendar` (from `react-day-picker`) inside `Popover` and tracks
the selected `DateRange` with `useState`, so it opts into client rendering.

## Usage

```tsx
"use client"

import { useState } from "react"
import { format } from "date-fns"
import { CalendarIcon } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Calendar } from "@/components/ui/calendar"
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover"
import { cn } from "@/lib/utils"
import type { DateRange } from "react-day-picker"

export function DatePickerDemo() {
  const [date, setDate] = useState<DateRange | undefined>({
    from: new Date(2026, 0, 12),
    to: new Date(2026, 0, 18),
  })

  return (
    <Popover>
      <PopoverTrigger asChild>
        <Button
          variant="outline"
          className={cn(
            "w-[260px] justify-start text-left font-normal",
            !date && "text-muted-foreground",
          )}
        >
          <CalendarIcon />
          {date?.from ? (
            date.to ? (
              <>
                {format(date.from, "LLL d")} – {format(date.to, "LLL d, y")}
              </>
            ) : (
              format(date.from, "LLL d, y")
            )
          ) : (
            <span>Pick a date range</span>
          )}
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-auto p-0" align="start">
        <Calendar
          mode="range"
          selected={date}
          onSelect={setDate}
          numberOfMonths={2}
        />
      </PopoverContent>
    </Popover>
  )
}
```

## Preview

<div className="my-4 flex flex-wrap items-center gap-4 rounded-lg border border-border p-6">
  <DatePickerDemo />
</div>

## Full API

[ui.shadcn.com/docs/components/date-picker](https://ui.shadcn.com/docs/components/date-picker)
